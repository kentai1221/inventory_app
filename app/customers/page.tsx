import { fetchMe } from '@/app/lib/data';

export default async function Page() {
  // const [me] = await Promise.all([fetchMe()]);
  // return <p>{`${process.env.BACKEND_URL}${me.image.url}`}</p>;
  return <></>
}

  
  